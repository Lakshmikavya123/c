#include<stdio.h>
int main()
{
int s_no;
scanf("%d",&s_no);
int subjects;
scanf("%d",&subjects);
int marks[subjects];
for(int i=0;i<subjects;i++)
{
scanf("%d",&marks[i]);
}
for(int i=0;i<subjects;i++);
{
scanf("%d",&marks[i]);
}
return 0;
}
